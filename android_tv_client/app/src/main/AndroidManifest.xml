<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Koneksi WebSocket ke server kasir -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />

    <!-- Floating Overlay Widget di atas game/aplikasi lain -->
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />

    <!-- Background Service agar koneksi WebSocket tetap hidup -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />

    <!-- Notifikasi service (Android 13+) -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- Layar tetap nyala saat Lockscreen -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <!-- Autostart service saat STB dinyalakan -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <application
        android:label="@string/app_name"
        android:icon="@mipmap/ic_launcher"
        android:theme="@style/Theme.RRBillingTV"
        android:usesCleartextTraffic="true"
        android:allowBackup="false"
        android:supportsRtl="false">

        <!-- Setup / konfigurasi (IP server, meja_id) -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTask">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!--
            Lockscreen fullscreen.
            Daftar sebagai HOME (default launcher) agar tombol HOME tidak bisa
            keluar dari lock — saat terkunci, HOME kembali ke layar lock ini.
            Aktivasi: Pengaturan -> Aplikasi -> Aplikasi default -> Aplikasi
            beranda (atau via adb, lihat README).
        -->
        <activity
            android:name=".lockscreen.LockScreenActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:excludeFromRecents="true"
            android:theme="@style/Theme.RRBillingTV.Fullscreen"
            android:configChanges="orientation|screenSize|keyboardHidden">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.HOME" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>

        <!-- Pemutar media promosi fullscreen (video/gambar dari server kasir) -->
        <activity
            android:name=".media.MediaActivity"
            android:exported="false"
            android:excludeFromRecents="true"
            android:launchMode="singleTask"
            android:theme="@style/Theme.RRBillingTV.Fullscreen"
            android:configChanges="orientation|screenSize|keyboardHidden" />

        <!-- Foreground Service: WebSocket + timer + overlay + lock -->
        <service
            android:name=".service.TvOverlayService"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="TV billing overlay timer and lock screen for rental PS" />
        </service>

        <!-- Autostart service saat boot -->
        <receiver
            android:name=".BootReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.QUICKBOOT_POWERON" />
            </intent-filter>
        </receiver>
    </application>
</manifest>
